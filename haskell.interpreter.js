haskell.interpreter.interpret = function(env, ast) {
	var result = 0;
	
	for (var i = 0; i < ast.length; i++) {
		var node = ast[i];
		
		switch (node) {
			case '+':
				break;
			case '-':
				break;
			case '*':
				break;
			case '/':
				break;
			default:
				break;
		}
	}
}